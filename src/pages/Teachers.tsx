import { Box, Typography } from "@mui/material";

export default function Teachers() {
  return (
    <Box sx={{ py: 4, textAlign: "center" }}>
      <Typography variant="h5">Teachers Page</Typography>
    </Box>
  );
}
